
<%= link_to "Back to all movies", movies_path %><br/>

<h2>
  <%= @actor.firstname %>  
  <%= @actor.lastname %>
</h2>

<% @actor.movies.each do |movie| %>
  <%= link_to movie.title, movie_path(movie) %>
  <%= raw '<br>' unless @actor == @actor.movies.last %>
<% end %>